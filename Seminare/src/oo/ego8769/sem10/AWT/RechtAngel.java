package oo.ego8769.sem10.AWT;

import java.awt.Component;

public class RechtAngel extends Component {
	int x;
	int y;
	int width;
	int height;

	/*
	 * @Override public void paint(Graphics d) { super.paint(d); for (RechtAngel
	 * rectangel : _rectangeles) { double xscale = getWidth() / size; double
	 * yscale = getWidth() / size;
	 * 
	 * } d.drawRect(x, y, width, height); ; }
	 */
}
